{
  "name": "mugstech-s3",
  "version": "1.0.0",
  "description": "Uploads and Retrieves files to/from S3 in an AWS lambda",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p tsconfig.build.json --declaration"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.188.0",
    "concat-stream": "^2.0.0"
  },
  "devDependencies": {
    "@types/concat-stream": "^2.0.0",
    "typescript": "^4.8.4"
  }
}
